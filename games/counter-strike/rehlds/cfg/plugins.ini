; Metamod plugin list. Order matters: ReUnion authenticates joining clients and
; must load before anything else.
; Installed by games/counter-strike/rehlds/install.sh.

linux addons/reunion/reunion_mm_i386.so
linux addons/amxmodx/dlls/amxmodx_mm_i386.so
